application.onAppStart = function()
{
        trace("Starting core.call.roundtrip");
}


Client.prototype.known_method = function(foo, bar, baz)
{
    if (foo != 'foo')
        return;

    if (bar != 'bar')
        return;

    this.call('client_method', null, 1, 2, 3);

    return 'ok';
}

